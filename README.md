# Ways to get the project in your workspace

1. Clone it down
  ```git clone "<<GIT_REPO_LINK>>"```

2. Install required packages
   ```cd <<PROJECT>>```
   ```npm install```

3. To run the project
   ```npm run dev```
